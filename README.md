# Requirements
- ruby 2.3.1
- bundler

# Installation instructions

- clone repo
- run `bundle` command
- locate `bin` directory
- execute `./wc`

# to run tests

- go to root directory of project and run `rspec spec`
